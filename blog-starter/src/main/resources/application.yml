server:
  port: 8000
spring:
  application:
    name: blog-starter
  profiles:
    active: dev
mybatis-plus:
  global-config:
    db-config:
      id-type: assign_id
logging:
  level:
    com.zeuslu.blog: debug
  pattern:
    dateformat: HH:mm:ss:SSS