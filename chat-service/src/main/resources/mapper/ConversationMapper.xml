<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeuslu.blog.chat.mapper.ConversationMapper">
    <select id="getConversationsByUserId" resultType="com.zeuslu.blog.domain.po.ChatConversation">
        select
            conversation.id,
            conversation.last_message_id,
            conversation.updated_at
        from
            tb_chat_conversation as conversation
                join tb_chat_conversation_user as conversation_user
        on
            conversation_user.conversation_id = conversation.id
        where
            conversation_user.user_id = #{userId}
        order by
            conversation.updated_at desc
    </select>
</mapper>
